{
  "name": "window-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "npm run db:migrate && electron -r babel-register .",
    "postinstall": "install-app-deps",
    "db:clean": "node_modules/.bin/sequelize db:migrate:undo:all && npm run db:migrate",
    "db:destroy": "rm db/*.sqlite3*",
    "db:migrate": "node_modules/.bin/sequelize db:migrate",
    "test": "NODE_ENV=test npm run db:clean && jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "active-win": "^3.0.0",
    "async": "^2.6.1",
    "electron": "^2.0.3",
    "process-list": "^1.2.3",
    "sequelize": "^4.38.0",
    "sequelize-cli": "^4.0.0",
    "sqlite3": "^4.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "electron-builder": "^20.18.0",
    "jest": "^23.4.1"
  }
}
